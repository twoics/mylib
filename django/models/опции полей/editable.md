Если указано значение `False` то поле нельзя редактировать из **админки**, а так же оно не будет там отображаться, так же как и в других формах

Так же эти поля пропускаются во время **проверки модели**
